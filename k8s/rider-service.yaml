apiVersion: apps/v1
kind: Deployment
metadata:
  name: rider-service
  namespace: rush
  labels: { app: rider-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: rider-service }
  template:
    metadata:
      labels: { app: rider-service }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
        prometheus.io/path: /metrics
    spec:
      containers:
        - name: rider-service
          image: rush/rider-service:latest
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 8081, name: http }]
          env:
            - { name: CONFIG_PATH, value: /config/config.yaml }
          envFrom:
            - secretRef: { name: rush-secrets }
          volumeMounts:
            - { name: config, mountPath: /config }
          livenessProbe:
            httpGet: { path: /healthz, port: http }
            initialDelaySeconds: 10
            periodSeconds: 20
          readinessProbe:
            httpGet: { path: /healthz, port: http }
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            requests: { cpu: 50m, memory: 64Mi }
            limits:   { cpu: 500m, memory: 256Mi }
      volumes:
        - name: config
          configMap:
            name: rush-config
            items: [{ key: config.yaml, path: config.yaml }]
---
apiVersion: v1
kind: Service
metadata:
  name: rider-service
  namespace: rush
spec:
  selector: { app: rider-service }
  ports:
    - { name: http, port: 8081, targetPort: 8081 }
