apiVersion: apps/v1
kind: Deployment
metadata:
  name: location-service
  namespace: rush
  labels: { app: location-service }
spec:
  # WebSocket sticky connections: each replica owns its own connections, no
  # cross-pod fan-out needed since Redis/Kafka is the shared state.
  replicas: 2
  selector:
    matchLabels: { app: location-service }
  template:
    metadata:
      labels: { app: location-service }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8083"
        prometheus.io/path: /metrics
    spec:
      containers:
        - name: location-service
          image: rush/location-service:latest
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 8083, name: http }]
          env:
            - { name: CONFIG_PATH, value: /config/config.yaml }
          envFrom:
            - secretRef: { name: rush-secrets }
          volumeMounts:
            - { name: config, mountPath: /config }
          livenessProbe:
            httpGet: { path: /healthz, port: http }
            initialDelaySeconds: 10
            periodSeconds: 20
          readinessProbe:
            httpGet: { path: /healthz, port: http }
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            requests: { cpu: 50m, memory: 64Mi }
            limits:   { cpu: 500m, memory: 256Mi }
      volumes:
        - name: config
          configMap:
            name: rush-config
            items: [{ key: config.yaml, path: config.yaml }]
---
apiVersion: v1
kind: Service
metadata:
  name: location-service
  namespace: rush
spec:
  # Long-lived WebSocket → sessionAffinity keeps a client pinned to one pod.
  sessionAffinity: ClientIP
  selector: { app: location-service }
  ports:
    - { name: http, port: 8083, targetPort: 8083 }
