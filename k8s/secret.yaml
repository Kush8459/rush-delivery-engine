# Dev-only secret. In real deployments use SealedSecrets / ExternalSecrets /
# your cloud KMS — do not commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: rush-secrets
  namespace: rush
type: Opaque
stringData:
  DE_POSTGRES_PASSWORD: postgres
  DE_AUTH_JWT_SECRET: dev-only-change-me
